# 기여 규약

> 이 저장소는 **GitHub Flow**를 쓴다. 장기 브랜치는 `main` 하나뿐이고, 나머지는 전부 짧게 살다 사라진다.

이 문서는 **어떻게 작업을 저장소에 반영하는가**를 고정한다.
무엇을 왜 만드는지는 [`docs/00-prd.md`](docs/00-prd.md)에 있다.

---

## 1. 저장소 구조

**모노레포다.** 백엔드·프론트엔드·문서가 한 저장소에 있다.

```
game-party-matcher/
├── docs/          설계 문서 (PRD, 기능 명세, 기술 명세, ERD)
├── backend/       PostgreSQL + Redis
├── frontend/
├── site/          설계 문서 정적 사이트
└── .github/
```

### 왜 나누지 않는가

언어가 다른 것은 분리 사유가 아니다. 빌드는 디렉터리별로 독립이고, CI는 path 필터로 갈린다.
실제 기준은 **변경이 얼마나 자주 BE/FE 경계를 넘는가**이고, 지금은 자주 넘는다.

| 조건 | 현재 |
|---|---|
| 별도 배포 단위 | 없다 |
| 영역별 전담자 | 없다. 셋이 같은 도메인을 만진다 |
| API 계약 안정성 | 설계 중이다. 6개월 내내 흔들린다 |

세 조건 중 하나라도 바뀌면 그때 분리한다. `git subtree split`으로 히스토리를 살려 떼어낼 수 있다.
합치는 쪽이 훨씬 비싸므로 나누지 않은 상태로 시작한다.

---

## 2. 브랜치 전략

```
main ─────●───────●───────●───────●──▶  항상 배포 가능
           ╲     ╱ ╲     ╱ ╲     ╱
            ●───●   ●───●   ●───●
         feat/12-  fix/15-  docs/18-
```

| 규칙 | 내용 |
|---|---|
| 장기 브랜치 | `main` 하나. `develop`·`release/*`·`deploy/*`는 만들지 않는다 |
| 작업 브랜치 | 이슈 하나당 하나. 머지 즉시 삭제 |
| 수명 | **3일 이내.** 더 걸리면 이슈를 쪼갠 것이 아니라 쪼개지 못한 것이다 |
| 분기점 | 항상 최신 `main`에서 딴다 |

`main`은 언제 잘라도 배포 가능한 상태여야 한다. 이게 이 전략의 유일한 전제다.

### 왜 Git Flow가 아닌가

Git Flow의 `develop`·`release/*`는 버전을 찍어 패키징 배포하던 모델에서 나왔다.
이 저장소는 `main` push마다 자동 배포되고 PR마다 프리뷰가 생성된다. 중간 통합 브랜치가 할 일이 없다.
`develop`을 두면 머지 커밋만 늘고 히스토리가 읽히지 않는다.

---

## 3. 작업 흐름

```
이슈 생성 → 브랜치 → 커밋 → PR → 리뷰 1명 승인 → Squash merge → 브랜치 삭제
```

**모든 작업은 이슈에서 시작한다.** 오타 수정도 예외가 아니다.
이슈 없이 시작한 작업은 기록에 의도가 남지 않고, 나중에 왜 그렇게 했는지 아무도 답하지 못한다.

```bash
git switch main && git pull
git switch -c feat/12-be-party-matching-api
# 작업, 커밋
git push -u origin feat/12-be-party-matching-api
gh pr create
```

### 이슈와 PR은 무엇이 다른가

| | 이슈 | PR |
|---|---|---|
| 뜻 | **하려는 일** | **해놓은 일** — 이 브랜치를 `main`에 합쳐달라는 요청 |
| 코드 변경 | 없다 | 있다. 브랜치가 붙어 있다 |
| 만드는 시점 | 작업 **전** | 작업 **후** |
| 리뷰 | 없다 | 승인 1명이 필요하다 |
| 닫는 법 | PR 본문의 `Closes #N`이 머지될 때 자동으로 닫는다 | 머지하거나 닫는다 |

**둘은 번호를 한 줄로 같이 센다.** 이슈 #1을 만든 뒤 PR을 올리면 그 PR은 #2다.
그래서 "다음 이슈 번호"를 미리 예상하고 브랜치를 파면 대체로 빗나간다. **이슈를 먼저 만들고 번호를 받아라.**

---

## 4. 브랜치 이름

```
<타입>/<이슈번호>-<스코프>-<영문-슬러그>
```

**무슨 종류 / 몇 번 이슈 - 어디 - 뭘 하는지** 네 조각이다. 실제 브랜치를 뜯어보면 이렇다.

```
feat / 12 - be - party-matching-api
 │     │     │          │
 │     │     │          └ 슬러그     무슨 작업인가
 │     │     └ 스코프                어느 영역인가
 │     └ 이슈번호                    왜 하는가
 └ 타입                              무슨 종류인가

docs / 9 - erd-architecture-sync     스코프 생략 — 문서라 BE/FE 구분이 없다
```

| 조각 | 무엇을 적나 | 규칙 |
|---|---|---|
| 타입 | 작업의 종류 | 아래 7종 중 하나. **새로 만들지 않는다** |
| 이슈번호 | 처리하는 이슈 | 숫자만. `#`는 붙이지 않는다 |
| 스코프 | 어느 영역인가 | `be` · `fe`. 모노레포라서 있는 조각이다. **걸치거나 공통이면 통째로 생략한다** |
| 슬러그 | 무슨 작업인가 | 영문 소문자와 하이픈만 |

### 타입 7종

이 일곱이 전부다. **커밋 메시지도 같은 7종을 쓴다**(5절).

| 타입 | 언제 쓰나 | 예시 |
|---|---|---|
| `feat` | 기능 추가 | `feat/12-be-party-matching-api` |
| `fix` | 버그 수정 | `fix/15-fe-proposal-card-expiry` |
| `docs` | 문서만 | `docs/18-erd-update` |
| `refactor` | 동작은 그대로, 구조만 개선 | `refactor/21-be-matching-service` |
| `test` | 테스트만 | `test/24-be-seat-contention` |
| `chore` | 빌드·설정·의존성 | `chore/27-github-actions` |
| `perf` | 성능 개선 | `perf/30-be-candidate-query` |

### 슬러그

영문 소문자와 하이픈만 쓴다. **한글·공백·대문자는 도구에 따라 깨진다.**

| | |
|---|---|
| `erd-architecture-sync` | 좋다 |
| `ERD업데이트` | 한글 — 도구에 따라 깨진다 |
| `erd update` | 공백 — 셸에서 인자가 쪼개진다 |
| `ERD-Update` | 대문자 — OS마다 대소문자 구분이 달라 충돌한다 |

---

## 5. 커밋 메시지

**타입은 영어, 설명은 한국어.** 기존 히스토리와 동일하다.

```
<타입>(<스코프>): <무엇을 했는가>

<왜 그렇게 했는가 — 필요할 때만>
```

```
feat(be): 파티 매칭 API 추가
fix(fe): 제안 카드 만료 표시 오류 수정
docs: 데이터 모델 ERD 추가
```

| 규칙 | 내용 |
|---|---|
| 제목 길이 | 50자 이내 |
| 제목 끝 | 마침표를 찍지 않는다 |
| 타입 | 브랜치 타입과 동일한 7종 |
| 스코프 | `be` · `fe` — 여러 영역에 걸치거나 공통이면 **괄호째 생략한다** |
| 본문 | **무엇**이 아니라 **왜**를 적는다. 무엇은 diff에 이미 있다 |

스코프는 `git log --oneline | grep '(be)'` 하나로 내 백엔드 작업만 뽑을 수 있게 해준다.

본문이 필요한 경우는 이렇다 — 대안을 버린 이유, 성능 트레이드오프, 임시 조치와 그 해소 조건.

```
fix: 예약 마감 경계 조건 수정

마감 시각 정각 요청이 반려됐다. 경계를 이상(>=)에서 초과(>)로 바꿨다.
정각 포함이 맞는지는 기획 확인 필요 — 확인 전까지 포함으로 둔다.
```

---

## 6. Pull Request

### 크기

| 변경 규모 | 판정 |
|---|---|
| ~300줄 | 적정 |
| 300~500줄 | 상한. 리뷰어에게 읽는 순서를 안내한다 |
| 500줄 초과 | **쪼갠다.** 리뷰가 통과 도장으로 변한다 |

자동 생성 파일·락파일은 줄 수에서 제외한다.

### 제목

커밋 메시지와 같은 형식을 쓴다. Squash merge 시 이 제목이 `main`의 커밋 메시지가 된다.

```
feat(be): 파티 매칭 API 추가
```

### 본문

`.github/pull_request_template.md`가 자동으로 뜬다. **이슈 번호 연결은 필수다.**

```
Closes #12
```

이 줄이 있어야 머지 시 이슈가 자동으로 닫히고, 6개월 뒤 이 코드가 왜 있는지 추적된다.

---

## 7. 리뷰

개발자는 3명이다. **승인은 1개면 된다 — 전원 동의가 아니다.**
내가 PR을 올리면 나머지 2명 중 먼저 본 사람이 승인하고 끝난다. 두 명을 다 기다리지 않는다.

| 항목 | 규칙 |
|---|---|
| 승인 | **1명 이상 필수** (나머지 개발자 2명 중 아무나) |
| 응답 시간 | 24시간 이내. 못 보면 PR에 그렇게 남긴다 |
| 셀프 머지 | 승인 없이는 불가. GitHub이 자기 PR 자기 승인을 막는다 |
| 코멘트 없는 승인 | 하지 않는다. 최소한 무엇을 확인했는지 적는다 |

### 멘토

멘토는 **승인 카운트에 넣지 않는다.** 멘토가 못 보는 동안 머지가 막히면 그건 규칙이 아니라 병목이다.
설계 결정이 바뀌는 PR — 스키마 변경, 불변식 변경, 외부 연동 추가 — 에만 리뷰를 요청한다.

### 코멘트 접두어

리뷰어가 강제성을 명시한다. 작성자가 무엇을 반드시 고쳐야 하는지 헷갈리지 않게 하기 위해서다.

| 접두어 | 의미 | 작성자 대응 |
|---|---|---|
| `P1` | 머지 전 반드시 수정 | 수정하거나, 반박하고 합의한다 |
| `P2` | 고치는 게 좋다 | 수정하거나, 후속 이슈를 만든다 |
| `P3` | 취향·제안 | 무시해도 된다 |
| `Q` | 질문 | 답한다 |

```
P1: 이 경로는 좌석 경합에서 정원 초과가 난다. 락 범위가 조회만 덮고 있다.
P3: 이 변수명은 candidateIds가 더 읽힌다.
Q: 이 배치가 재실행되면 어떻게 되나?
```

---

## 8. 머지

| 규칙 | 내용 |
|---|---|
| 방식 | **Squash merge만.** Merge commit·Rebase merge는 저장소 설정에서 비활성화했다 |
| 결과 | `main`의 커밋 1개 = 이슈 1개 = PR 1개 |
| 커밋 메시지 | PR 제목과 본문이 그대로 들어간다. 그래서 PR 제목이 커밋 규약을 따라야 한다 |
| 머지 후 | 브랜치는 **자동 삭제된다** |
| 충돌 해소 | 작업 브랜치에서 `main`을 rebase 하거나 merge 한다. `main`에서 하지 않는다 |

Squash를 고정하는 이유는 히스토리 자체가 산출물이기 때문이다.
`main`의 로그가 "기능 1줄 = 커밋 1개"로 읽히면 프로젝트 진행이 로그만으로 설명된다.

---

## 9. main 보호 규칙

저장소 설정으로 강제한다. 합의가 아니라 잠금이다.

| 설정 | 값 | 상태 |
|---|---|---|
| 직접 push | 금지 | 적용됨 |
| PR 필수 | 예 | 적용됨 |
| 승인 필요 수 | 1 | 적용됨 |
| 새 커밋 시 승인 무효화 | 예 | 적용됨 |
| 관리자도 적용 | 예 | 적용됨 |
| force push | 금지 | 적용됨 |
| 브랜치 삭제 | 금지 | 적용됨 |
| 선형 히스토리 강제 | 예 | 적용됨 |
| 리뷰 대화 해소 필수 | 예 | 적용됨 |
| CI 통과 필수 | 예 | **보류** — 워크플로(`convention`·`links`)는 있고, 필수 검사 등록만 남았다 |

관리자도 예외를 두지 않는다. 예외가 한 번 열리면 규칙은 그날로 끝난다.

**선형 히스토리 강제**는 Squash 전용 설정과 짝이다. 저장소 설정에서 Merge commit과 Rebase merge를
비활성화했으므로 `main`에는 머지 커밋이 생길 수 없다.

**리뷰 대화 해소 필수**는 `P1` 코멘트가 답변 없이 묻히는 것을 막는다. 열린 스레드가 하나라도 남으면
머지 버튼이 잠긴다. 반박해서 합의했다면 스레드를 닫으면 된다.

### 승인자가 전원 부재일 때

개발자는 3명이다. 두 명이 동시에 부재하면 **승인 1명 규칙이 성립하지 않는다.**
2026-08-19에 실제로 이 상황이 발생해 문서 PR 세 개가 멈췄고, 보호를 임시로 풀어 머지했다.

위에 "예외를 두지 않는다"고 적어놓고 예외를 열었다. 그러면 선언을 반복할 것이 아니라
**절차로 만들어야 한다.** 위험한 것은 예외 자체가 아니라 **흔적 없는 예외**다.
언제 풀고 언제 되돌렸는지 남지 않으면 다음번에는 되돌리는 것을 잊는다.

**먼저 기다린다.** 부재가 하루 이틀이면 PR을 열어둔 채로 둔다. 대부분은 급하지 않다.

**그래도 막히면 아래를 지킨다.**

| 단계 | 내용 |
|---|---|
| 1 | 왜 기다릴 수 없는지 PR 본문에 적는다 |
| 2 | **승인 필요 수만 `1` → `0`으로 낮춘다.** 보호 자체를 끄거나 직접 push를 열지 않는다 |
| 3 | 머지한다 |
| 4 | **같은 날 안에 `1`로 되돌린다** |
| 5 | 언제 풀고 언제 되돌렸는지 팀 채널에 남긴다 |

**`docs`·`chore`로 한정한다.** 코드가 들어가는 타입(`feat`·`fix`·`refactor`·`perf`·`test`)은
승인 없이 넘기지 않는다. 리뷰를 건너뛴 코드가 `main`에 들어가면 그 뒤로는 되돌릴 근거도 사라진다.
문서가 틀리면 다음 PR에서 고치면 되지만, 코드는 그사이에 다른 코드가 그 위에 쌓인다.

**부재는 미리 알린다.** 휴가·시험 등으로 24시간 이상 리뷰가 어려우면 팀 채널에 남긴다.
남기지 않은 부재는 이 예외의 근거가 되지 않는다.

---

## 10. 하지 않는 것

- `main`에 직접 push
- 이슈 없는 PR
- 500줄 넘는 PR을 "급해서" 머지
- 리뷰 코멘트 없는 승인
- 여러 목적을 한 PR에 섞기 — 기능 추가와 리팩터링은 나눈다
- 커밋 메시지에 `수정`, `ㅇㅇ`, `asdf` 같은 무의미한 문구
- 머지된 브랜치 방치
- 보호를 풀고 **그날 안에 원복하지 않기**
- 승인 없이 코드 머지 — 예외는 `docs`·`chore`뿐이다
