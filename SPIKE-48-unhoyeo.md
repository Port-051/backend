# 스파이크 #48 — 실시간 매칭 코어 (여운호)

> `spike/48-unhoyeo`. 규약 10절에 따라 `main`에 머지하지 않는다.
> 판정일에 이 문서를 놓고 설명한다.

## 무엇을 만들었나

명세 [01번](docs/01-functional-spec-mvp.md) **4장 자동 매칭 + 5장 즉시 매칭 제안·수락**.
Core API가 없으므로 요청 접수 REST·SSE·명단에서 지우기까지 실시간 매칭 안에 뒀다(05 "이번 단계의 범위").

**DB를 쓰지 않는다.** JPA·Flyway·PostgreSQL은 의존성에 없다.

```
src/main/java/com/port051/queuemate/
├── contract/     계약 고정부 — req: JSON · Redis 키 · enum. 여기는 내 자율이 아니다
├── matching/     4장 — 후보 제외 · 포지션 배정 · 결정적 FCFS · 틱 루프 · 선점
├── offer/        5장 — 제안 수명 · 수락/거절 · 만료 · 실패 조합 억제
├── sse/          8.1 — 인스턴스 간 팬아웃
├── result/       판정 게이트가 읽을 확정 기록
└── api/          계약이 정한 API 표면
```

---

## 계약대로 맞춘 것

| | 확인 |
|---|---|
| Redis 키 셋 | `mq:{queue}:{size}` · `req:{id}` · `claim:{id}` |
| `req:` JSON | 13개 필드 이름·타입 그대로. **필드를 더하지도 않았다** |
| API 7개 | `POST /api/match-requests` · `GET`·`DELETE` · accept · decline · `/api/events` · `/api/me/state` |
| SSE 이벤트 7종 | `OFFER_CREATED` `OFFER_RESPONSE_UPDATED` `OFFER_DECLINED` `OFFER_EXPIRED` `PARTY_CONFIRMED` `REQUEST_CANCELLED` `MATCH_FAILED` |
| 스택 | Java 21 · Spring Boot 4.1.0 · Redis(Lettuce) |
| 부하 | k6 `constant-arrival-rate` |

**최대 대기시간을 `req:` JSON에 넣지 않았다.** 계약이 필드를 고정했으므로 서버 설정
(`queuemate.matching.max-wait`)으로 뺐다. 계약에 없는 필드를 얹으면 사람마다 부하 스크립트가 갈린다.

---

## 갈라놓은 것 — 내 비교 축

### 1. 선점 단위와 범위 — **요청 하나, 전원 all-or-nothing, Lua 원자**

경합의 실체가 "이 사람이 두 파티에 들어가는가"라 요청이 곧 좌석이다.
파티 단위로 잡으려면 아직 존재하지 않는 파티에 락을 걸어야 한다.

후보를 하나씩 `SET NX`로 잡으면 중간까지 성공하고 실패하는 상태가 남는다. 그러면 다른 인스턴스는
이미 잡힌 앞쪽을 보고 물러나고, 부하 구간에서 **아무도 성립시키지 못하는 교착이 반복된다.**
그래서 `claim_all.lua` 한 덩어리로 전부 잡거나 아무것도 안 잡는다.

### 2. 선점 TTL — **5초. 제안 수명(20초)보다 짧다**

제안이 만들어지는 순간 참가자가 명단에서 빠지므로, 선점이 실제로 필요한 구간은
**후보를 고른 뒤 제안을 만들기까지의 짧은 순간뿐**이다.
길게 잡으면 매처가 죽었을 때 그 요청들이 TTL만큼 통째로 얼어붙는다.

02 1.3이 못박은 **"락은 최적화이고 정합성은 제약이 보장한다"**를 코드로 지킨 결과다 —
선점이 전부 사라져도 `confirm_party.lua`가 INV-2·3·4를 막는다.
4단계 과제인 "락 만료 중 작업 지속"이 여기서 그대로 재현 가능하다.

### 3. 대기 명단 — **Sorted Set, score = 신청 시각, member = 19자리 제로패딩 requestId**

01 4.3의 전순서가 `(신청 시각, 요청 ID)`다. 점수를 신청 시각으로 두면 1차 정렬이 공짜이고,
점수가 같을 때 Redis가 member를 **사전순**으로 비교하므로 자릿수를 맞추면 사전순 == 숫자순이 된다.
**두 정렬키가 자료구조 하나로 표현된다.**

List로 두면 취소된 요청을 중간에서 지우는 것이 O(n)이고, 무엇보다 신청 시각이 같은 요청의 순서가
삽입 시점에 정해져 인스턴스마다 갈린다.

### 4. 명단 읽기 — **앞에서 batchSize(500)만큼 자르고 MGET으로 한 번에**

대기자가 수천이어도 한 틱에 성립시킬 파티는 몇 개 안 된다. 대신 **뒤에 있는 요청은 앞이 비워질
때까지 기다린다** — 대기 시간과 처리량의 교환이고 `batchSize`가 그 다이얼이다.

### 5. 틱 주기 — **200ms** (02 1.4의 제안값은 1초)

02 6.4의 목표가 "제안 전달 지연 p95 1초 이하"다. 틱이 1초면 최악의 경우 제안 생성만으로 예산을
다 쓴다. 200ms면 나머지 800ms를 전파에 쓸 수 있다. 대가는 빈 조회다.

### 6. 파티션 — **나누지 않는다.** 두 인스턴스가 같은 명단을 전부 훑는다

02 1.4가 이것과 파티션 단일 라이터를 비교 대상으로 남겼다. 전자를 골랐다.
큐·정원 조합이 여덟인데 인스턴스가 둘이면, 파티션을 나누는 순간 한 대가 죽었을 때
그쪽 파티션의 매칭이 통째로 멈춘다. 대신 두 인스턴스가 같은 후보를 집는 낭비가 생기고,
그 낭비를 선점이 얼마나 줄이는지가 이 축의 결론이다.

### 7. 포지션 배정 — **Kuhn 이분 매칭(가부 판정) + 완전 탐색(주 포지션 최대화)**

매칭 루프는 후보를 하나씩 얹으며 "이 조합이 가능한가"를 조합마다 묻는다. 그건 Kuhn으로 싸게 끊는다.
실제 배정은 파티가 성립한 뒤 한 번만 필요하므로 그때만 완전 탐색으로 **주 포지션 수를 최대화**한다.
정원이 최대 5, 포지션도 5라 경우의 수가 120을 넘지 않아 근사 없이 최적해가 나온다.

### 8. 확정의 원자성 경계 — **Lua 하나. 검사 셋 + 쓰기 전부**

`confirm_party.lua`가 INV-3 → INV-4 → INV-2를 차례로 보고, 하나라도 걸리면 아무것도 쓰지 않는다.
PostgreSQL의 `UNIQUE(request_id)` · 부분 유니크 인덱스 · `EXCLUDE USING gist`가 하던 일을
한 자리에 모았다.

**어느 제약이 막았는지 구분해서 돌려준다**(`1` / `0`=INV-3 / `-1`=INV-4 / `-2`=INV-2).
셋은 원인이 완전히 다르다 — INV-3은 두 인스턴스가 같은 요청으로 파티를 짜다 하나가 진 것이라
**정상 경로**이고, INV-4는 배정 로직의 버그이며, INV-2는 사전 필터가 놓친 것이다.
구분하지 않으면 로그에 "확정이 막혔다"만 남아 무엇이 일하고 있는지 알 수 없다.

### 9. SSE vs WebSocket — **SSE**

이 구간에서 오가는 것은 전부 서버 → 클라이언트 단방향이다. 클라이언트가 보내는 것(수락·거절·취소)은
이미 REST에 있고 그쪽은 멱등성과 상태코드가 필요해 HTTP가 맞다. WebSocket으로 얻는 양방향은 쓸 데가
없고, 잃는 것은 분명하다 — 업그레이드가 붙어 LB·프록시 설정이 늘고 재연결·하트비트를 직접 짜야 한다.

---

## 판정 게이트 — 확정 결과를 Redis에 남기고 스캔한다

이슈 #48의 미체크 항목 **"판정 게이트를 무엇으로 하나"**에 대한 답이다.

```
parties               파티 id 집합 — 스캔 진입점
party:{id}            queue · targetSize · startAt · endAt · purpose · voiceParty
party:{id}:members    requestId -> "포지션|userId"
member:{requestId}    requestId -> partyId      ← INV-3을 강제하는 자리
busy:{userId}         ZSET score=종료시각 member="partyId|시작시각"  ← INV-2
```

`load-test/verify-invariants.py`가 SCAN으로 계수한다.

**이 대체가 잃는 것을 먼저 적는다.** 02가 검출을 DB에 둔 이유는 부하를 넣는 도구와 위반을 세는
도구를 분리해 "부하 도구가 놓친 위반"이라는 사각을 없애려는 것이었다(04 4.4). 쓰기와 읽기가 같은
Redis면 그 분리가 없다 — **확정을 아예 기록하지 않는 버그가 있으면 위반이 0건으로 나온다.**
그래서 검증 스크립트는 `확정 파티 수`를 반드시 같이 출력하고, 그 수가 부하 스크립트의 `confirmed`와
맞는지 사람이 대조해야 한다.

### ⚠️ 이 스파이크에서는 INV-1을 잴 수 없다

02 2장이 명시했다 — **"INV-1은 7.4 빈자리 공개 모집 경로에서만 발생한다. 전원 수락 확정 경로에는
좌석을 놓고 경쟁하는 사람이 없어 경합이 생기지 않는다."** 이슈 #48이 6·7장을 범위에서 뺐으므로
정원 초과는 **구조적으로 0건**이다.

**0건을 "제어가 잘 됐다"로 읽으면 안 된다. 잴 경로가 없는 것이다.**
README가 프로젝트의 본체라고 선언한 문제 1이 이 스파이크의 판정 대상에서 빠져 있다는 뜻이고,
이건 판정일에 팀이 결정할 사안이다.

---

## 측정 결과 (2026-08-28)

측정 환경 — MacBook(Apple Silicon), Docker Compose, 인스턴스 2대 + nginx LB + Redis 1대.
씨드 `20260828`, 티어 분포 `real`, 자유 랭크 정원 5, `constant-arrival-rate`, open model.

| 항목 | 30/s · 22초 | 60/s · 25초 | 목표 (02 6.4) | |
|---|---|---|---|---|
| 신청 수 | 628 | 1,354 | — | |
| 확정 | 320 | 650 | — | |
| 매칭 성립률 | 51.0 % | 48.0 % | — | 조건 전부 필수 처리 |
| 매칭 요청 생성 p95 | **3.2 ms** | **5.2 ms** | 200 ms 이하 | 통과 |
| 제안 전달 지연 p95 | **55 ms** | — | 1000 ms 이하 | 통과 |
| 확정 전달 지연 p95 | 59 ms | — | — | |
| INV-2 · 3 · 4 | **0건** | **0건** | 0건 | 통과 |
| INV-1 | 0건 | 0건 | 0건 | **경로 없음 — 위 참조** |

대조 — 60/s 실행에서 확정 파티 130개 × 정원 5 = 참가자 650명이고, k6가 센 확정도 650이다.
이 대조가 맞아야 "위반 0건"을 읽을 수 있다.

**제약이 실제로 발동하는 것도 봤다.** 앞선 실행의 로그에 `확정이 INV-3에 막혔다`가 남았다 —
두 인스턴스가 같은 요청으로 파티를 짜다 하나가 진 것이고, 진 쪽 참가자는 재대기했다.
02 1.4가 "이 경합은 INV-3이 흡수한다"고 한 그대로다.

**이 수치를 그대로 02 6.4의 달성으로 읽지 말 것.** 목표가 유효한 조건은 "동시 대기자 200명 구간"인데
위 실행은 그 구간까지 올리지 않았다. 성립률 곡선(02 6.3)의 10/30/50/100/200 구간 측정은 아직 없다.

### 자동 테스트

36개 전부 통과. Testcontainers로 진짜 Redis를 띄운다.
동시성은 실제로 스레드를 붙여 잰다 — 32스레드가 같은 후보를 선점하면 **승자 정확히 1명**,
24스레드가 같은 요청으로 확정을 시도하면 **성공 정확히 1건**.

---

## 부하 도구에서 나온 사실 두 가지

### 1. stock k6에 SSE 모듈이 없다 — 04 4.1의 확인 결과

04 4.1이 **"0단계에서 먼저 확인할 것"**으로 지정한 검사를 돌렸다. k6 v2.2.0에서
`k6/x/sse` · `k6/experimental/sse` · `k6/net/sse` **전부 없다**. xk6로 직접 빌드해야 한다.
(`k6/experimental/websockets`는 있다.)

04 4.5가 대비해 둔 두 선택지 중 **두 번째**를 골랐다 — "시나리오를 쪼갠다. 제안 전달 지연은
SSE 수신까지만 재고, 수락은 별도 측정으로 분리한다."

- `load-test/matching-load.js` — REST만. 제안 도착은 폴링으로 안다
- `load-test/sse-latency-probe.mjs` — SSE로 제안 전달 지연만 잰다 (Node `--experimental-eventsource`)

**폴링으로 바뀌면서 잃는 것** — 부하 스크립트의 "신청→제안" 수치에는 폴링 주기가 섞여 있다.
그걸 서버 지연으로 읽으면 안 된다. 성립률과 불변식은 폴링과 무관하므로 그대로 쓴다.

> 04 4.5가 "어느 쪽이든 0단계 안에서 판명되므로 그때 이 문서를 고친다"고 했다. **판명됐다.**
> `docs/04-tech-stack.md` 4장을 고쳐야 한다 — main 트랙 작업이다.

### 2. Spring Boot 4.1은 Jackson 3을 쓴다

`tools.jackson.databind.ObjectMapper`다. `com.fasterxml.jackson.databind`가 아니다.
어노테이션(`@JsonInclude` 등)만 `com.fasterxml.jackson.annotation`에 남아 있다.
04 2.2가 경고한 "생태계 시차"가 실제로 나온 지점이다.

`spring-boot-starter-web`이 JSON 라이브러리를 딸려 보내지 않는 것도 Boot 4의 변화라
`spring-boot-starter-json`을 명시해야 한다.

---

## 부하가 잡아낸 결함 하나

첫 측정에서 **INV-2 위반 3,925건**이 나왔다.

원인은 내 구현 누락이다. 01 4.1이 후보 제외 사유로 **"이미 시간이 겹치는 확정 파티에 속해 있다"**를
적어 두었는데, 나는 "확정 시 명단에서 지워지니 괜찮다"고 보고 넘겼다. 그건 **같은 요청**이 두 번
들어가는 것(INV-3)만 막는다. 사용자가 파티에 속한 채 **새 요청**을 만들면 또 배정된다.

고친 방식은 02 2장이 PostgreSQL에 두려던 것과 같은 모양이다 —
`busy:{userId}` ZSET에 점유 구간을 남기고, `confirm_party.lua`가 쓰기 직전에 겹침을 검사한다.
후보 단계의 사전 필터도 넣었지만 그건 낭비를 줄이는 용도이고, 실제로 막는 것은 Lua 쪽이다.
02 2장이 "**사전 검증은 경합 조건에서 누락이 발생하고 배제 제약은 발생하지 않는다**"고 한 그대로다.

고친 뒤 **3,925건 → 0건.**

> 02 6.1이 "대응 전 위반이 0건이면 그 단계는 아직 시작하지 않은 것으로 본다"고 했다.
> 이건 의도한 재현은 아니지만 **대응 전 N건 → 대응 후 0건**의 형태를 갖췄다.

---

## 돌리는 법

```bash
# 인스턴스 2대 + LB + Redis
docker compose up -d --build

# 테스트 (Testcontainers로 Redis를 따로 띄운다)
./gradlew test

# 부하 — 프로브를 먼저 띄우고 k6를 건다
cd load-test
node --experimental-eventsource sse-latency-probe.mjs &
ARRIVAL_RATE=30 DURATION=22s TIER_DISTRIBUTION=real k6 run matching-load.js

# 판정 게이트
python3 load-test/verify-invariants.py
```

`TIER_DISTRIBUTION`은 `uniform`과 `real` 둘 다 돌린다(02 6.3).
`SEED`를 고정하면 같은 부하가 재현된다.

**도커 런타임 주의** — OrbStack·Colima는 소켓이 `/var/run/docker.sock`에 없어서 Testcontainers가
데몬을 못 찾는다. `build.gradle.kts`가 흔한 경로를 훑어 `DOCKER_HOST`를 넣어 준다.
Docker 29는 API 1.40 미만을 거절하므로 `api.version`도 거기서 지정한다.

---

## 남은 것

- [ ] 성립률 곡선 — 동시 대기자 10/30/50/100/200 구간 (02 6.3). 지금은 한 지점만 쟀다
- [ ] `uniform` 분포 정식 측정. 지금은 `real`만 표에 있다
- [ ] 티어 밴드 표(`application.yml`) 검증. 01 5.2가 "구현 전 현재 규칙을 재확인할 것"이라고 했고 **아직 안 했다**
- [ ] `real` 티어 분포 비율도 미확인 근사값이다
- [ ] 재회 목록 우선 매칭(01 4.3 3번) — 계약 JSON에 필드가 없어 뺐다
- [ ] 차단 쌍 제외(01 4.1) — 같은 이유
